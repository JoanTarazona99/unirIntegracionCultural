# FastAPI and API server (REQUIRED)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-dotenv==1.0.0

# Data processing (REQUIRED for keyword search)
numpy==1.24.3

# Telegram bot (REQUIRED for bot)
python-telegram-bot==20.3
aiohttp==3.9.1

# HTTP client (REQUIRED for bot backend calls)
requests==2.31.0

# Translation (OPTIONAL - graceful fallback if not installed)
google-trans-new==1.1.9

# ==================== OPTIONAL DEPENDENCIES ====================
# Install these for enhanced features. The app works without them.

# Semantic search (OPTIONAL - falls back to keyword search)
# pip install sentence-transformers torch

# TTS/STT (OPTIONAL - text-to-speech and speech-to-text)
# pip install gTTS SpeechRecognition

# LLM integration (OPTIONAL - local LLM via Ollama)
# pip install ollama httpx
# Requires: ollama server running locally with a model pulled

# Database (OPTIONAL - for persistent storage)
# pip install psycopg2-binary sqlalchemy pandas
