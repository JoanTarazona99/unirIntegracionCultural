# FastAPI and API server
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Logging
structlog==23.2.0

# Data processing
numpy==1.24.3
pandas==2.1.3

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
aiosqlite==0.20.0  # async SQLite (optional - falls back to memory)

# Cache
redis==5.0.1  # optional - falls back to LRU if unavailable
asyncpg==0.29.0  # optional - async PostgreSQL driver

# Telegram bot
python-telegram-bot==20.3
aiohttp

# HTTP and networking
requests==2.31.0

# Translation
google-trans-new==1.1.9

# Lexical retrieval (BM25) - pure Python, CPU-friendly, always available
rank-bm25==0.2.2

# Semantic search (optional - falls back to keyword/BM25 if not installed)
sentence-transformers==2.2.2
torch==2.0.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# TTS/STT (optional - text-to-speech and speech-to-text)
gTTS==2.3.2
SpeechRecognition==3.10.0

# LLM integration (optional - local LLM via Ollama)
httpx
python-multipart